# Copy this file to .env and adjust the values.
# .env is gitignored — each machine keeps its own paths.

# Where the generated project code should be written.
# Defaults to this repo's directory if not set.
# TARGET_DIR=../my-new-project

# Python interpreter to use.
# PYTHON=python3
